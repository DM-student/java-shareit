CREATE TABLE IF NOT EXISTS USERS (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY,
        PRIMARY KEY (id),
        email VARCHAR(320) NOT NULL UNIQUE,
        name VARCHAR(64) NOT NULL
);

CREATE TABLE IF NOT EXISTS ITEMS (
        id BIGINT GENERATED BY DEFAULT AS IDENTITY,
        PRIMARY KEY (id),
        name VARCHAR(32) NOT NULL,
        description VARCHAR(378) NOT NULL,
        available BIT NOT NULL
);

CREATE TABLE IF NOT EXISTS users_to_items(
	user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
	item_id BIGINT NOT NULL REFERENCES items(id) ON DELETE CASCADE,
	CONSTRAINT unique_users_to_items_entry UNIQUE (user_id, item_id)
);

CREATE TABLE IF NOT EXISTS comments(
    id BIGINT GENERATED BY DEFAULT AS IDENTITY,
	user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
	item_id BIGINT NOT NULL REFERENCES items(id) ON DELETE CASCADE,
	text VARCHAR(378) NOT NULL,
	created TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS bookings(
    id BIGINT GENERATED BY DEFAULT AS IDENTITY,
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    item_id BIGINT NOT NULL REFERENCES items(id) ON DELETE CASCADE,
	state VARCHAR(20) NOT NULL,
	start_date TIMESTAMP NOT NULL,
	end_date TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS requests(
    id BIGINT GENERATED BY DEFAULT AS IDENTITY,
    PRIMARY KEY (id),
    user_id BIGINT NOT NULL REFERENCES users(id) ON DELETE CASCADE,
    description VARCHAR(378) NOT NULL,
	created TIMESTAMP NOT NULL
);

CREATE TABLE IF NOT EXISTS requests_to_items(
    request_id BIGINT NOT NULL REFERENCES requests(id) ON DELETE CASCADE,
	item_id BIGINT NOT NULL REFERENCES items(id) ON DELETE CASCADE,
	CONSTRAINT unique_items_to_requests_entry UNIQUE (request_id, item_id)
);

